<!DOCTYPE html>
<html>
<head>
    <title></title>

    <meta name="viewport" content="width=user-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css" rel="stylesheet">
    <link rel="stylesheet" href="http://cdn.leafletjs.com/leaflet-0.7.2/leaflet.css" />
    <!--[if lte IE 8]><link rel="stylesheet" href="http://cdn.leafletjs.com/leaflet-0.7.2/leaflet.ie.css" /><![endif]-->

    <link rel="stylesheet" href="../../stylesheets/leaflet-sidebar.css" />
    <link rel="stylesheet" href="../../stylesheets/geodesa.css" />
    <style>
        body {
            padding: 0;
            margin: 0;
        }
        html, body, #map {
            height: 100%;
            font: 10pt "Helvetica Neue", Arial, Helvetica, sans-serif;
        }
        .lorem {
            font-style: italic;
            color: #AAA;
        }
    </style>
</head>
<body>
  <div class="panel panel-info nuevoformulario">

            <div class="panel-heading">
              <h3 class="panel-title"><p>Provider: </p></h3>
            </div>
            <div class="panel-body">
              <form class="form-inline" method="post" action="/providers/<%=provider._id %>?_method=put">
                <label for="nameProvider">Nombre</label><br>
                <input class="form-control" id="nameProvider" type="text" value="<%= provider.name %>" name="name"><br><br>
                <label for="cifProvider">Cif</label><br>
                <input class="form-control" id="cifProvider" type="text" value="<%= provider.cif %>" name="cif" readonly><br><br>
                <label for="phoneProvider">Teléfono</label><br>
                <input class="form-control" id="phoneProvider" type="text" value="<%= provider.phone %>" name="phone"><br><br>
                <label for="faxProvider">Fax</label><br>
                <input class="form-control" id="faxProvider" type="text" value="<%= provider.fax %>" name="fax"><br><br>
                <label for="mailProvider">Email</label><br>
                <input class="form-control" id="mailProvider" type="text" value="<%= provider.mail %>" name="mail"><br><br>
                <label for="addressProvider">Direccion</label><br>
                <input class="form-control" id="addressProvider" type="text" value="<%= provider.address %>" name="address"><br><br>
                <label for="postal_codeProvider">Código postal</label><br>
                <input class="form-control" id="postal_codeProvider" type="text" value="<%= provider.postal_code %>" name="postal_code"><br><br>
                <label for="provinceProvider">Provincia</label><br>
                <input class="form-control" id="porvinceProvider" type="text" value="<%= provider.province %>" name="province"><br><br>

                <br><br>
                <button type="submit" class="btn btn-info">Modificar</button>
              </form>
            </div>

  </div>
  <hr>
  <div class="backButton">
    <a href="<%= redir %>" class="btn btn-info btn-lg active btn-block">Volver</a>
  </div>
  <script src="http://cdn.leafletjs.com/leaflet-0.7.2/leaflet.js"></script>
  <script src="../../js/leaflet-sidebar.js"></script>

</body>
