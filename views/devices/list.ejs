<!DOCTYPE html>
<html>
<head>
    <title></title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css" rel="stylesheet">
    <link rel="stylesheet" href="http://cdn.leafletjs.com/leaflet-0.7.2/leaflet.css" />
    <!--[if lte IE 8]><link rel="stylesheet" href="http://cdn.leafletjs.com/leaflet-0.7.2/leaflet.ie.css" /><![endif]-->

    <link rel="stylesheet" href="../stylesheets/leaflet-sidebar.css" />
    <link rel="stylesheet" href="../stylesheets/geodesa.css" />
    <style>
        body {
            padding: 0;
            margin: 0;
        }
        html, body, #map {
            height: 100%;
            font: 10pt "Helvetica Neue", Arial, Helvetica, sans-serif;
        }
        .lorem {
            font-style: italic;
            color: #AAA;
        }
    </style>
</head>
<body>
  <div class="panel panel-info panel-listado">
      <div class="panel-heading">
            <h3 class="panel-title"><p>Listado de dispositivos</p></h3>
      </div>
      <form class="form-inline" action="/list" method="get" >
         <table class="table">
           <thead>
             <tr>
               <th>
                 Nombre
               </th>
               <th>
                 Localidad
               </th>
               <th>
                 Localización
               </th>
               <th>

               </th>
             </tr>

           </thead>
           <tbody>
           <% for (var i=0; i< listDevices.length; i++) { %>
               <tr>
                 <td>
                   <%= listDevices[i].name %>
                 </td>
                 <td>
                  <%= listDevices[i].province %>
                 </td>
                 <td>
                   <%= listDevices[i].location.coordinates %>
                 </td>
                 <td>

                    <!--<a href="#" class="btn btn-info btn-sm active">-->
                      <form action="/devices/<%= listDevices[i]._id %>" method="get">
                          <button type="submit" class="btn btn-success">Ver</button>
                      </form>
                 </td>
                 <td>
                       <form action="/devices/update/<%= listDevices[i]._id %>" method="put">
                          <!-- Indicates caution should be taken with this action -->
                          <button type="submit" class="btn btn-warning">Editar</button>
                      </form>
                 </td>
                 <td>
                      <form action="/devices/delete/<%= listDevices[i]._id %>" method="delete">
                          <!-- Indicates a dangerous or potentially negative action -->
                          <button type="submit" class="btn btn-danger">Borrar</button>
                      </form>
                 </td>
                </tr>
            <% } %>
            </tbody>
          </table>
        </form>
        <form action="../" method="get">
            <!-- Indicates a dangerous or potentially negative action -->
            <button type="submit" class="btn btn-primary .btn-block">volver</button>
        </form>

    </div>

    <script src="http://cdn.leafletjs.com/leaflet-0.7.2/leaflet.js"></script>
    <script src="../js/leaflet-sidebar.js"></script>
</body>
</html>
